import java.util.Arrays;

public class GeneralKnowledge {
    public static void main(String[] args) {
//        Notes (seperated by blank lines):-

//        use equals() method instead of '==' for checking whether two Strings or Wrapper classes are the same

        Integer[] arr = new Integer[2];
        System.out.println(Arrays.toString(arr)); // output: [null, null]
        Arrays.fill(arr, 0);
    }
}
